machine:
  java:
    version: oraclejdk8

test:
  pre:
    - ls -ls
    - pwd
  post:
    - echo $PATH

checkout:
  pre:
    - echo 'THIS IS IN checkout:pre'
    - pwd
  post:
    - git checkout origin cibuild
    - echo 'THIS IS IN checkout:post'
    - pwd